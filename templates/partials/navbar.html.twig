<nav class="navbar navbar-expand-lg bg-body-tertiary">
	<div class="container-fluid">
		<a class="navbar-brand" href="{{ path('app_home') }}">Allociné</a>
		<button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
			<span class="navbar-toggler-icon"></span>
		</button>
		<div class="collapse navbar-collapse" id="navbarNav">
			{% if not app.user %}
				<ul class="navbar-nav d-flex flex-row-reverse w-100">
				{% else %}
					<ul class="navbar-nav">
					{% endif %}
					{% if not app.user %}
						<li class="nav-item p-1">
							<a class="btn btn-primary" href="{{ path('app_login') }}">Login</a>
						</li>
						<li class="nav-item p-1">
							<a class="btn btn-dark" href="{{ path('app_register') }}">Register</a>
						</li>
					{% else %}
						<li class="nav-item p-1">
							<a class="nav-link" href="{{ path('app_actor_index') }}">Actors</a>
						</li>
						<li class="nav-item p-1">
							<a class="nav-link" href="{{ path('app_movie_index') }}">Movies</a>
						</li>
					</ul>
					<ul class="navbar-nav d-flex flex-row-reverse w-100">
						<li class="nav-item p-1">
							<a class="btn btn-danger" href="{{ path('app_logout') }}">Logout</a>
						</li>
					{% endif %}
				</ul>
			</div>
		</div>
	</nav>
